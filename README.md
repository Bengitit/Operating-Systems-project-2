Here is a breakdown of my code logic, line by line. I used the “int count = 0;“ to establish a local counter set to zero. This will reset each thread back to zero. Then I calculate the size for each thread with this equation “int part = n1 / NUM_THREADS;”. N1 represents how many characters are in the first text section, and NUM_THREADS represents 10. We then calculate which thread to start with “int start = t * part;”. An “int end” variable was created to store the last position. An if else statement was created to atone the following conditions. In this statement. “if(t == NUM_THREADS -1) end = n1 - n2;” it checks if we are at the last thread. If we are it minuses the difference between the two strings. This is to ensure that the last possible position n2 can appear in n1. Example: s1 = 100 & s2 = 5. The last possible place for s2 to start is 95. This would prevent a error like it starting at 96 hench 96–99 would not have enough characters left to match all of s2. For the else section. For the else section “end = (t + 1) * part + n2 - 1;”, this would include all other threads. “(t + 1) * part” gives the normal end of the thread's section and the “+ n2 – 1" allows the thread to look a little past the boundary. This is needed as a match may start near the end of a partition and continue into the next partition. The extra “n2 - 1” character allows the thread to complete the comparison. With this for loop “for(int i = start; i <= end && i <= n1 - n2; i++)” we can loop through the assigned range with i representing the starting position of s2 inside s1. For this loop I added an AND Logic. This is to prevent “i" from going past the end and will keep it bound of the two lengths. “int match = 1;” is declared as I am going to assume that the word we are searching for is already in the that thread. To confirm this it will compare every character of s2 in s1 with this for loop “for(int j = 0; j < n2; j++)”. In that for loop it will Compare every character in both strings with “if(s1[i + j] != s2[j])”. If any character is different, the substring is not a match, and match will become 0 causing it to break. If it is successful, the count will increase by 1 for that particular thread. The results will save using “countArray[t] = count;”. Note that Each thread has its own count. The “return count;” will send the number of matches back to the thread function “(calSubStringThread)”. In summary, num_substring() splits the big string s1 among 10 threads, searches for s2 in each section, counts the matches, stores each thread's count, and returns the local result. 
